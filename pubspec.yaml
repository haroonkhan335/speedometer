name: speedometer
description: A Flutter library to show-case your data in form of speedometers.
version: 0.0.1
homepage: https://github.com/haroonkhan335/speedometer

environment:
  sdk: ">=2.18.4 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
